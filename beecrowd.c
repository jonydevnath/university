#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main(void)
{

    // double A, R;
    // scanf("%lf", &R);
    // double pi = 3.14159;
    // A = pi * pow(R, 2);

    // printf("A=%.4lf\n", A);
    // return 0;

    // int num1, num2, num3;
    // scanf("%d %d %d", &num1, &num2, &num3);

    // int arry[3] = {num1, num2, num3};
    // int temp = 0;

    // for (int i = 0; i < 3; i++)
    // {
    //     for (int j = i + 1; j < 3; j++)
    //     {
    //         if (arry[i] > arry[j])
    //         {
    //             temp = arry[i];
    //             arry[i] = arry[j];
    //             arry[j] = temp;
    //         }
    //     }
    // }

    // int tempnum1 = num1;
    // int tempnum2 = num2;
    // int tempnum3 = num3;

    // num1 = arry[0];
    // num2 = arry[1];
    // num3 = arry[2];

    // printf("%d\n%d\n%d\n\n", num1, num2, num3);
    // printf("%d\n%d\n%d\n", tempnum1, tempnum2, tempnum3);

    // return 0;

    // // Time Conversion
    // int N, H, M, S;
    // scanf("%d", &N);

    // // Number of Hours
    // H = N / 3600;
    // // // Remaining Seconds
    // S = N % 3600;
    // // // Number of Minutes
    // M = S / 60;
    // // // Remaining Seconds
    // S = N % 60;

    // printf("%d:%d:%d\n", H, M, S);

    // return 0;

    // int product1, price1, product2, price2;
    // float paid;

    // scanf("%d %d %d %d", &product1, &price1, &product2, &price2);

    // printf("VALOR A PAGAR: R$ %.2f", paid);
    // return 0;


    int a, b, c, ab, bc;
    scanf("%d %d %d", &a, &b, &c);
    ab = (a + b + abs(a - b)) / 2;
    bc = (ab + c+ abs(ab - c)) / 2;
    printf("%d eh o maior\n", bc);
    return 0;

}
// 1 problem solved
// MATH
// IEE

/*
take 3000 tk; book + calculator = 150+250 = 400 (mine) + 1900 = 2300 == 600 tk;

I have 3600 tk -- 500 tk nogad + 2000 tk still have; 500 + 600 tk = 1100tk take;
-----------------------------
tanspotation 30 tk
book = 150 tk;
sadi 250 book tk give 400 others +400 wifi have to give;
belt = 250, slipper = 350;
medicine = 85;
potato = 40 1kg, chile = 10 tk;
dal = 50 half kg; onion = 40 half kg;
bread = 45 tk;
water = 60 tk;
total = 1360 tk
---------------------------
*/